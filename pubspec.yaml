name: easy_isolate
description: An abstraction of the isolate providing an easy way to work with different threads
version: 1.3.0
homepage: https://www.britto.dev

environment:
  sdk: ">=2.12.0 <3.0.0"

dev_dependencies:
  test: ^1.17.12
